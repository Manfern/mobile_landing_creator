<h1 class="text-center">Редактирования преимущества</h1>
<%=  form_for([@page,@advantage],html: { method: :put,multipart: true }) do |f| %>
    <%= render 'form', f: f %>
<% end %>

<!--url: offer_path(@offer)-->